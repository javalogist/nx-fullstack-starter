{
  "name": "web",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/web",
  "projectType": "application",
  "tags": [],
  "targets": {
    "serve": {
      "executor": "@nx/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "web:build",
        "dev": true,
        "turbo": true,
        "port":4001
      },
      "configurations": {
        "development": {
          "buildTarget": "web:build:development",
          "dev": true,
          "port":4001
        },
        "production": {
          "buildTarget": "web:build:production",
          "dev": false,
          "port":4001
        }
      }
    },
    "build": {
      "executor": "@nx/next:build",
      "outputs": [
        "{options.outputPath}"
      ],
      "options": {
        "outputPath": "dist/web"
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "build-standalone": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "npx nx run web:build",
          "node scripts/build-nextjs-standalone.js web"
        ],
        "parallel": false
      }
    }
  }
}
